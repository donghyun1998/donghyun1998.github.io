export default function Category() {
  // 여기다가 카테고리를 불러오는 코드를 넣어야 함
  return (
    <div className="bg-gray flex min-w-max max-w-max flex-col gap-4 p-4">
      <div>all (400)</div>
      <div>CS (7)</div>
      <div>Algorithm (300)</div>
      <div>42seoul (10)</div>
      <div>web (10)</div>
    </div>
  )
}
